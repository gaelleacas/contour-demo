apiVersion: apps/v1
kind: Deployment
metadata:
  name: playmo-v1
  namespace: playmo
  labels:
    app: playmo-v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: playmo-v1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: playmo-v1
    spec:
      containers:
      - name: playmo-v1
        image: eu.gcr.io/khaly-221308/demo-contour
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
        env:
        - name: VERSION
          value: "Home v1"
        - name: TEAM
          value: "Playmo"
        - name: LABEL
          value: "TEAM"
        - name: PORT
          value: "8080"
        - name: ROUTING
          value: "false"
---
apiVersion: v1
kind: Service
metadata:
  name: playmo-v1
  namespace: playmo
  labels:
    app: playmo-v1
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  sessionAffinity: None
  selector:
    app: playmo-v1
